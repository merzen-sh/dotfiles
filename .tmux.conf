# lead
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

#keybind
bind f display-popup -E -w 80% -h 80% -d "#{pane_current_path}"
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind r source-file ~/.tmux.conf \; display-message "Config reloaded..."

set -s default-terminal "tmux-256color"
set -g mouse on
set -g base-index 1 
setw -g pane-base-index 1
set -g status-position top

#custom
set -g status-bg black
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
setw -g bell-action none
set-option -g extended-keys always

# clock mode
setw -g clock-mode-colour yellow

# copy mode
setw -g mode-style 'fg=black bg=cyan bold'

# panes
set -g pane-border-style 'fg=cyan'
set -g pane-active-border-style 'fg=yellow'

# statusbar
set -g status-position top
set -g status-justify left
set -g status-style 'fg=cyan'

set -g status-left '#[fg=cyan,bg=black]#[fg=cyan]'
set -g status-left-length 10

set -g status-right '#[fg=cyan]#[fg=black, bg=cyan] 󱑉 %H:%M'
set -g status-right-length 50

setw -g window-status-format '#[bg=black,fg=cyan]  #I:#W #[fg=black,bg=black]'

setw -g window-status-separator ""

setw -g window-status-current-style 'fg=black bg=cyan bold'
setw -g window-status-current-format '#[fg=cyan,bg=black]#[bg=cyan,fg=black] #I:#W #[fg=cyan,bg=black]'

# messages
set -g message-style 'fg=cyan bg=black bold'
